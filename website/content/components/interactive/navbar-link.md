# NavbarLink

`NavbarLink` is the link-level leaf used inside `Navbar`. It preserves email-specific anchor markup and can resolve relative links against a parent `base-url`.

<ComponentMetaTable tag="mj-navbar-link" />