# SocialElement

`SocialElement` is the leaf entry inside `Social`. It resolves inherited defaults, network presets, and custom icon assets into the final icon-and-label markup.

<ComponentMetaTable tag="mj-social-element" />