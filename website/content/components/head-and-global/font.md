# Font

`Font` declares a named web font import. VJML only emits the import markup when that font is actually referenced by the rendered content or inline style rules.

<ComponentMetaTable tag="mj-font" />