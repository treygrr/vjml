# Style

`Style` adds CSS to the email head. Use plain `Style` for document-level rules and `inline="inline"` when matching selectors should become inline styles on rendered elements.

<ComponentMetaTable tag="mj-style" />