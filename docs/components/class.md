# Class

`Class` defines a reusable `mj-class` preset inside `Attributes`. Apply it to components with the `mj-class` prop when you want reusable MJML-native defaults.

<ComponentMetaTable tag="mj-class" />