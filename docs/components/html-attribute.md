# HtmlAttribute

`HtmlAttribute` defines a single attribute assignment inside a `Selector`. Use it together with `HtmlAttributes` when you need exact HTML attributes on the final output.

<ComponentMetaTable tag="mj-html-attribute" />