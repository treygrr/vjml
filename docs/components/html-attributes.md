# HtmlAttributes

`HtmlAttributes` lets you attach literal HTML attributes to rendered output using selectors. Use it when you need data attributes or similar DOM-level markers in the final email HTML.

<ComponentMetaTable tag="mj-html-attributes" />