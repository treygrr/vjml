# Mjml

`Mjml` is the document root. It coordinates top-level language and direction settings, collects head content, and chooses the body that becomes the rendered email shell.

<ComponentMetaTable tag="mjml" />