# Carousel

`Carousel` renders the interactive image carousel shell, responsive controls, and Outlook fallback markup. It works with `CarouselImage` children.

<ComponentMetaTable tag="mj-carousel" />