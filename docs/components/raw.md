# Raw

`Raw` injects literal content into the head, body, or before the document doctype. Use the `html` or `content` prop when the raw fragment includes tags that Vue would otherwise treat as side-effect markup.

<ComponentMetaTable tag="mj-raw" />