# Head

`Head` is renderless. Use it to collect preview text, title, fonts, styles, selector attributes, and raw head fragments before the final document is built.

<ComponentMetaTable tag="mj-head" />